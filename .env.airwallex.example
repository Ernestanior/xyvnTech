# Airwallex 支付集成配置
# 请将这些配置添加到您的 .env 文件中

# =============================================================================
# Airwallex API 配置
# =============================================================================

# Airwallex Client ID (在 Airwallex Webapp 中获取)
# 登录 https://www.airwallex.com/app/ 后，在 Settings > API Keys 中创建
AIRWALLEX_CLIENT_ID=your_client_id_here

# Airwallex API Secret Key (在 Airwallex Webapp 中获取)
# 创建 API Key 时会同时生成 Client ID 和 API Secret
AIRWALLEX_API_KEY=your_api_secret_here

# Airwallex 环境配置
# 测试环境使用 'staging'，生产环境使用 'production'
AIRWALLEX_ENV=staging

# 服务器端口 (用于本地开发)
PORT=3000

# =============================================================================
# 前端配置 (在 .env.local 中添加，供客户端使用)
# =============================================================================

# Airwallex 客户端 ID (与后端相同，但需要暴露给前端)
NEXT_PUBLIC_AIRWALLEX_CLIENT_ID=your_client_id_here

# Airwallex 环境配置 (供前端使用)
NEXT_PUBLIC_AIRWALLEX_ENV=staging

# =============================================================================
# 如何获取 Airwallex API Keys:
# =============================================================================
# 1. 访问 https://www.airwallex.com/app/ 登录您的账户
# 2. 点击左侧菜单 "Settings" -> "API Keys"
# 3. 点击 "Create API Key" 按钮
# 4. 输入名称 (如: Arvix Web Integration)
# 5. 选择权限: 至少勾选 "Payments" 和 "Payment Intents"
# 6. 点击 "Create"，系统会生成 Client ID 和 API Secret
# 7. 立即保存 API Secret，因为它只会显示一次
# 8. 将 Client ID 和 API Secret 填入上方配置
#
# 注意: 
# - 测试环境使用 staging，不会扣款，用于测试
# - 生产环境使用 production，真实扣款，切换前请确保测试通过
# - 不要将 .env 文件提交到版本控制，已添加到 .gitignore
